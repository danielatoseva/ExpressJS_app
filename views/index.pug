extends layout

block content
  h1= title
  p Welcome to #{title}
  table.table.table-striped
      tr
        th Day 
        th Type

      each n in weekdays
        tr
          td= n.day 
          td= n.type 

  button.btn.btn-primary Click me
